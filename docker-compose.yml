services:
  nextjs-app:
    build:
      context: ./diagnosis-app 
    ports:
      - "21014:3000"            
    environment:
      - NODE_ENV=production
    volumes:
      - /app/node_modules       
    restart: unless-stopped
    networks:
      - nextjs-network          

networks:
  nextjs-network:
    driver: bridge
